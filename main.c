#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
    int *p;
    int i, strike;
    int one, two, three;
    printf("0~9까지 3자리 숫자를 입력하시오 : ");
    scanf("%d %d %d", &one, &two, &three);
    printf("%d %d %d\n", one, two, three);
}